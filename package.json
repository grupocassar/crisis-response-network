{
  "name": "crisis-response-network",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm install && npm run dev",
    "start:backend": "cd backend && npm install && node index.js",
    "test:webhook": "node scripts/test-webhook-dispatch.js",
    "db:ping": "node scripts/conectar_db.js",
    "db:sql": "bash scripts/ejecutar_sql.sh"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
